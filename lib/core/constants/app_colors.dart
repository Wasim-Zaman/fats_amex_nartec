import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primaryBlue = Color(0xFF000080); // Navy blue from your design
  static const secondaryBlue =
      Color(0xFF4169E1); // Royal blue for secondary elements
  static const mintGreen = Color(0xFFE8F5E9); // Light mint green background
  static const darkMintGreen = Color(0xFF0A1F0D); // Dark mint green background

  // Background colors
  static const backgroundLight = Color(0xFFFFFFFF);
  static const backgroundDark = Color(0xFF121212);

  // Surface colors
  static const surfaceLight = Color(0xFFF0F8FF);
  static const surfaceDark = Color(0xFF1E1E1E);

  // Text colors
  static const textDark = Color(0xFF000000);
  static const textLight = Color(0xFFFFFFFF);

  // Error/Success colors
  static const error = Color(0xFFB00020);
  static const success = Color(0xFF4CAF50);

  // Card colors
  static const cardLight = Color(0xFFFFFFFF);
  static const cardDark = Color(0xFF1E1E1E);

  // other colors
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
}
